Remark:
  meta:
    verbose_name: "Remark"
    verbose_name_plural: "Remarks"

  fields:
    remark_id:
      type: String
      unique: true
      max_length: 100

    text:
      type: String
      max_length: 500

    device:
      type: OneToOne
      to: Device
      nullable: false


