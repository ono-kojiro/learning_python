---
User:
  meta:
    verbose_name: "User"
    verbose_name_plural: "Users"

  fields:
    user_id:
      type: String
      unique: true
      max_length: 100

    name:
      type: String
      max_length: 255

    email:
      type: String
      max_length: 255

