---
Comment:
  meta:
    verbose_name: "Comment"
    verbose_name_plural: "Comment"

  fields:
    comment_id:
      type: String
      unique: true
      max_length: 100

    text:
      type: String
      max_length: 500
