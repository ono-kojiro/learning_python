---
OS:
  meta:
    verbose_name: "OS"
    verbose_name_plural: "OS"

  fields:
    os_id:
      type: String
      unique: true
      max_length: 100

    text:
      type: String
      max_length: 500

    device:
      type: OneToOne
      to: Device
      nullable: false


